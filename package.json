{
  "name": "css-image",
  "version": "0.2.0",
  "description": "",
  "main": "index.js",
  "repository": "http://github.com/lexich/css-image",
  "scripts": {
    "test": "npm run eslint && npm run jscs && istanbul test _mocha --report html -- test/*.js --reporter spec",
    "coveralls": "npm run eslint && npm run jscs && istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage",
    "testgulp": "npm run jscs && npm run eslint && node_modules/.bin/gulp test",
    "jscs": "node_modules/.bin/jscs ./*.js test/*.js",
    "eslint": "node_modules/.bin/eslint ./*.js test/*.js",
    "precommit": "npm test",
    "prepush": "npm test",
    "postmerge": "npm install"
  },
  "keywords": [
    "css",
    "node"
  ],
  "author": {
    "name": "Efremov Alex",
    "email": "lexich121@gmail.com",
    "url": "https://github.com/lexich"
  },
  "license": "MIT",
  "dependencies": {
    "lodash": "^3.10.0",
    "node-css": "0.1.0"
  },
  "devDependencies": {
    "babel-eslint": "^4.0.5",
    "chai": "^3.0.0",
    "coveralls": "^2.11.3",
    "eslint": "^1.0.0",
    "eslint-config-airbnb": "0.0.7",
    "eslint-plugin-react": "^3.1.0",
    "gulp": "^3.9.0",
    "gulp-istanbul": "^0.10.0",
    "gulp-mocha": "^2.1.3",
    "husky": "^0.9.1",
    "istanbul": "^0.3.17",
    "jscs": "^2.0.0",
    "mocha": "^2.2.5",
    "mocha-lcov-reporter": "0.0.2"
  }
}
